@AGENTS.md

## 新增商品時，請讀取並遵守 `.claude/commands/add-item.md` 的完整 SOP。

此 SOP 涵蓋：商品名稱規則、官網連結格式（bandai-hobby.net 優先）、圖片下載方式、reviews / youtube 搜尋規則、裝箱標籤格式、部署後通知方式。

**每次新增商品前必讀該檔案，不得跳過任何步驟。**

## 編輯前必做：同步線上狀態

每次開始工作前，先執行以下指令確保本地與線上同步，避免覆蓋他人修改：

```bash
git pull origin main
```

若有衝突（merge conflict），先解決衝突再繼續，不可強制覆蓋。
